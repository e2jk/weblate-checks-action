name: Fuzzing

# Deliberately independent of ci.yml: no `needs:`/status-check relationship,
# not configured as a required check in the ruleset, and triggered by push
# to main (i.e. after a PR merges) rather than on the PR itself — so it
# cannot block a PR merge or a release even mechanically. A crash here
# surfaces two ways — the job summary (crash traceback inline) and the
# Security tab (SARIF, deduped by rule+location like CodeQL/bandit/pip-audit
# already are in this repo) — but every finding needs human triage before
# it's known to be a real, reachable bug. Adapted from OpenHangar's
# fuzzing.yml, trimmed to this repo's single-module layout (no separate
# runtime.txt to install — the harnesses only need requirements-fuzz.txt,
# since weblate_checks_to_sarif.py itself has no runtime dependencies).

on:
  push:
    branches: [main] # runs after a PR merges, not on every PR push — matches
    # the pattern most other workflows here use (codeql.yml, scorecard.yml)
  schedule:
    - cron: '0 4 * * 3' # weekly, Wednesday 04:00 UTC — deeper batch run
  workflow_dispatch:
    inputs:
      full_run:
        description: 'Run the full/large weekly fuzzing budget (~20 min per harness) instead of the quick ~2 min budget'
        type: boolean
        default: false

permissions: read-all  # zizmor: ignore[excessive-permissions] intentional: workflow default is read-only, the job below opts into exactly the write scopes it needs

concurrency:
  group: ${{ format('{0}-{1}', github.workflow, github.ref) }}
  cancel-in-progress: true

jobs:
  fuzz:
    name: Fuzz (${{ matrix.harness }})
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      security-events: write # crash SARIF upload to the Security tab
    strategy:
      fail-fast: false
      matrix:
        harness:
          - fuzz_scrape_checks
          - fuzz_parse_locations
          - fuzz_slugify
          - fuzz_fetch_scheme_validation
          - fuzz_component_languages_response
          - fuzz_flagged_units_response
    env:
      # Routed through a shell env var rather than interpolated directly
      # into run: blocks (${{ matrix.harness }} inline in a script is a
      # template-injection footgun zizmor's pedantic persona flags — these
      # values are hardcoded in the matrix above, not attacker-controlled,
      # but there's no reason not to use the safe pattern anyway).
      HARNESS: ${{ matrix.harness }}
      FULL_RUN: ${{ inputs.full_run }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false

      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: '3.12'
          cache: 'pip'
          cache-dependency-path: requirements-fuzz.txt

      - name: Set up venv
        run: |
          python -m venv .venv
          .venv/bin/pip install --quiet --require-hashes -r requirements-fuzz.txt

      - name: Create corpus directory
        run: mkdir -p "fuzz/corpus/${HARNESS}"

      - name: Restore fuzz corpus
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: fuzz/corpus/${{ matrix.harness }}
          key: fuzz-corpus-${{ matrix.harness }}-${{ github.run_id }}
          restore-keys: |
            fuzz-corpus-${{ matrix.harness }}-

      - name: Run fuzzer
        # ~120s per harness after each merge to main (all matrix legs run in
        # parallel, so wall time doesn't grow as more harnesses are added
        # later); a much deeper ~20 min budget on the weekly scheduled batch
        # run, or on a manual dispatch with the "full_run" input checked.
        #
        # continue-on-error + an explicit failure step at the end (rather
        # than letting this step fail the job outright) so every later step
        # still runs regardless of outcome — in particular so a SARIF gets
        # uploaded for this category on *every* run, not just a crashing
        # one. GitHub Code Scanning only auto-closes an alert when a fresh
        # upload for the same category no longer contains it; uploading
        # only on failure (the previous design) meant a since-fixed crash
        # never got a chance to close itself.
        id: fuzz
        continue-on-error: true
        run: |
          seconds=120
          if [ "$GITHUB_EVENT_NAME" = "schedule" ] || [ "$FULL_RUN" = "true" ]; then
            seconds=1200
          fi
          .venv/bin/python "fuzz/${HARNESS}.py" \
            "fuzz/corpus/${HARNESS}" \
            "-max_total_time=${seconds}"

      - name: Save fuzz corpus
        # Always save, even if the fuzz run above failed (found a crash) —
        # the corpus growth from this run is still worth keeping.
        if: always()
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: fuzz/corpus/${{ matrix.harness }}
          key: fuzz-corpus-${{ matrix.harness }}-${{ github.run_id }}

      - name: Reproduce crash deterministically
        # A single, non-fuzzing re-run against the crash file Atheris just
        # wrote, to capture a clean traceback for the summary/SARIF steps
        # below (the fuzzing run's own log is noisy and non-deterministic).
        if: steps.fuzz.outcome == 'failure'
        run: |
          crash_file=$(find . -maxdepth 1 -name 'crash-*' -print -quit)
          if [ -n "$crash_file" ]; then
            .venv/bin/python "fuzz/${HARNESS}.py" "$crash_file" > crash-repro.log 2>&1 || true
          fi

      - name: Write job summary
        if: steps.fuzz.outcome == 'failure' && hashFiles('crash-repro.log') != ''
        run: |
          {
            echo "### 🐛 Fuzz crash: \`${HARNESS}\`"
            echo
            echo '```'
            cat crash-repro.log
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Build SARIF (crash or clean)
        # Crash SARIF when there's a reproducible crash log; otherwise a
        # clean, empty-results SARIF for this harness's category — see the
        # "Run fuzzer" step's comment for why the clean case matters too.
        env:
          FUZZ_OUTCOME: ${{ steps.fuzz.outcome }}
        run: |
          if [ "${FUZZ_OUTCOME}" = "failure" ] && [ -f crash-repro.log ]; then
            .venv/bin/python .github/scripts/fuzz_crash_to_sarif.py \
              "$HARNESS" crash-repro.log crash.sarif
          else
            .venv/bin/python .github/scripts/fuzz_crash_to_sarif.py \
              "$HARNESS" --clean crash.sarif
          fi

      - name: Upload results to GitHub Security tab
        uses: github/codeql-action/upload-sarif@e4fba868fa4b1b91e1fdab776edc8cfbe6e9fb81 # v4
        with:
          sarif_file: crash.sarif
          category: fuzz-${{ matrix.harness }}

      - name: Upload crash reproducer
        if: steps.fuzz.outcome == 'failure'
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: ${{ matrix.harness }}-crash
          path: crash-*
          if-no-files-found: ignore

      - name: Fail the job if the fuzzer crashed
        # The actual failure signal, deferred to here now that "Run fuzzer"
        # itself no longer fails the job directly (continue-on-error above).
        if: steps.fuzz.outcome == 'failure'
        run: exit 1
